name: Initialize

on:
  workflow_dispatch:

defaults:
  run:
    shell: pwsh

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v3
      - name: Setup
        run: |
          (Resolve-Path "${{ github.action_path }}/../..").Path >> $Env:GITHUB_PATH
      - name: Test
        run: |
          Get-Rekt
